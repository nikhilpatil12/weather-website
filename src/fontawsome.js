// import the library
import { library } from '@fortawesome/fontawesome-svg-core';

// import your icons
import { faCode, faHighlighter, faLocation, faLocationCrosshairs, faLocationDot, faLocationPin, faSearch } from '@fortawesome/free-solid-svg-icons';


library.add(
  faCode,
  faHighlighter,
  faLocation,
  faLocationCrosshairs,
  faLocationPin,
  faSearch,
  faLocationDot
);